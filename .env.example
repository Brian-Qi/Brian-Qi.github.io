# ============================================================
# 环境变量示例（复制为 .env.local 后按需填写）
# ============================================================
# ⚠️ 重要：本项目基于 Vue CLI，`VUE_APP_*` 的变量会被**打进前端 JS 包**，
#    任何人打开 DevTools 都能看到。**绝不要**把任何秘密放进来。
#
# ⛔ 绝对不要这样写：
#    VUE_APP_ADMIN_PASSWORD=...     # 管理员口令只在你自建后端校验
#    VUE_APP_SECRET_KEY=...         # 前端没有“密钥”可言
#    VUE_APP_SUPABASE_ANON_KEY=...  # 已不再使用 Supabase
#
# ✅ 留言墙：数据由自建后端 /api（Express + MySQL）提供，
#    管理员登录由后端校验并下发 token（前端只存 sessionStorage）。
#    因此**本仓库默认不需要任何环境变量**。
#
# 如将来确有可公开的前端配置（如版本号、开关），按此格式添加：
# VUE_APP_VERSION=1.0.0
# VUE_APP_DEBUG_MODE=false
